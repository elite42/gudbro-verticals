---
phase: 09-code-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/wellness/frontend/app/staff/[slug]/page.tsx
  - apps/tours/frontend/components/booking/BookingForm.tsx
  - apps/workshops/frontend/app/about/page.tsx
autonomous: true

must_haves:
  truths:
    - 'Tours compiles with tsc --noEmit without any soups DB type exclusion in tsconfig'
    - 'Wellness /staff/[slug] page has a visible text back link navigating to /staff'
    - 'Zero href=# placeholder links exist in Tours and Workshops codebases'
  artifacts:
    - path: 'apps/wellness/frontend/app/staff/[slug]/page.tsx'
      provides: 'Text back link to staff list'
      contains: 'Back to Team'
    - path: 'apps/tours/frontend/components/booking/BookingForm.tsx'
      provides: 'Real link instead of href=#'
      pattern: 'no href="#" present'
    - path: 'apps/workshops/frontend/app/about/page.tsx'
      provides: 'Real links instead of href=#'
      pattern: 'no href="#" present'
  key_links:
    - from: 'apps/wellness/frontend/app/staff/[slug]/page.tsx'
      to: '/staff'
      via: 'Link component href'
      pattern: 'href="/staff"'
---

<objective>
Resolve three quick code-fix requirements: verify Tours TypeScript compilation (CFIX-01), add visible back link to Wellness staff detail page (CFIX-03), and replace all placeholder href="#" links in Tours and Workshops (CFIX-05).

Purpose: Close 3 of 5 tech debt items from v1.0 with minimal, targeted changes.
Output: Clean compilation, visible navigation, and zero placeholder links.
</objective>

<execution_context>
@/Users/gianfrancodagostino/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gianfrancodagostino/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-code-fixes/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Tours TypeScript compilation (CFIX-01)</name>
  <files>apps/tours/frontend/tsconfig.json</files>
  <action>
    Run `cd apps/tours/frontend && npx tsc --noEmit` and confirm exit code 0.
    Verify that `tsconfig.json` exclude array contains only `["node_modules"]` -- no soups DB type exclusion exists.
    Research found this is already passing. This task is verification only -- no code changes needed.
    If somehow tsc fails, fix the actual type errors (do NOT add exclusions or suppressions).
  </action>
  <verify>
    `cd apps/tours/frontend && npx tsc --noEmit` exits with code 0.
    `grep -c "soups" apps/tours/frontend/tsconfig.json` returns 0.
  </verify>
  <done>Tours compiles cleanly with tsc --noEmit and no soups-related tsconfig exclusions exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add back link to Wellness staff detail page (CFIX-03) and replace placeholder links (CFIX-05)</name>
  <files>
    apps/wellness/frontend/app/staff/[slug]/page.tsx
    apps/tours/frontend/components/booking/BookingForm.tsx
    apps/workshops/frontend/app/about/page.tsx
  </files>
  <action>
    **CFIX-03: Wellness back link**
    In `apps/wellness/frontend/app/staff/[slug]/page.tsx`, add a visible text back link below the hero image section (near line 313, in the profile info area). Use this pattern:

    ```tsx
    <Link href="/staff" className="inline-flex items-center gap-1 text-sm text-muted-foreground hover:text-foreground transition-colors">
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M15 18l-6-6 6-6"/></svg>
      Back to Team
    </Link>
    ```

    Place it as the first element inside the content area below the hero, before the staff member name. Match the existing Wellness styling patterns (check the "not found" state which already has a "Browse Team" link for reference). Use Phosphor Icons if already imported, otherwise a simple chevron SVG is fine.

    **CFIX-05: Replace placeholder links**

    1. `apps/tours/frontend/components/booking/BookingForm.tsx` (line ~505): Replace `href="#"` on "Terms of Service" link. Since no /terms route exists, change the `<a>` to a `<button>` that opens an inline expandable section or simple alert with placeholder terms text. Alternatively, link to `/contact` if that route exists. The key constraint: NO href="#".

    2. `apps/workshops/frontend/app/about/page.tsx` (lines ~391 and ~460): Replace both `href="#"` instances ("Join Our Network" and "Partner With Us" CTAs). Change these to link to `/contact` if that route exists in Workshops. If no /contact route exists, use `href="mailto:info@gudbro.com"` or remove the Link wrapper and make them non-link CTAs with an onClick that scrolls to a contact section. The key constraint: NO href="#".

    For all replacements: check what routes exist in the respective apps before choosing link targets. Prefer existing routes over creating new ones.

  </action>
  <verify>
    `grep -rn 'href="#"' apps/tours/frontend/ apps/workshops/frontend/` returns zero results.
    `grep -n "Back to Team" apps/wellness/frontend/app/staff/\[slug\]/page.tsx` returns a match.
    `cd apps/wellness/frontend && npx next build` completes without errors.
    `cd apps/tours/frontend && npx next build` completes without errors.
    `cd apps/workshops/frontend && npx next build` completes without errors.
  </verify>
  <done>
    Wellness /staff/[slug] has a visible "Back to Team" text link navigating to /staff.
    Zero href="#" placeholder links remain in Tours and Workshops codebases.
    All three apps build successfully.
  </done>
</task>

</tasks>

<verification>
1. `cd apps/tours/frontend && npx tsc --noEmit` -- exit code 0
2. `grep -rn 'href="#"' apps/tours/frontend/ apps/workshops/frontend/` -- zero results
3. `grep -n "Back to Team" apps/wellness/frontend/app/staff/\[slug\]/page.tsx` -- match found
4. All three apps build: tours, wellness, workshops
</verification>

<success_criteria>

- CFIX-01: Tours tsc --noEmit passes with exit code 0, no soups exclusion in tsconfig
- CFIX-03: Wellness staff detail page shows visible "Back to Team" text link
- CFIX-05: Zero href="#" in Tours and Workshops (was 3, now 0)
  </success_criteria>

<output>
After completion, create `.planning/phases/09-code-fixes/09-01-SUMMARY.md`
</output>
